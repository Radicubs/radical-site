import type { Metadata } from "next";import { Navbar } from "@/components/sections/navbar";import { Footer } from "@/components/sections/footer";import { AnimatedSection } from "@/components/ui/animated-section";import { corporateSponsors,individualSponsors } from "@/data/sponsors";import { site } from "@/data/site";
export const metadata:Metadata={title:"Sponsors | Radicubs"};
export default function SponsorsPage(){return <main><Navbar/><section className="page-hero"><div className="wrap"><AnimatedSection><p className="eyebrow">Support Radicubs</p><h1>Sponsors</h1><p className="section-copy">As an independent student-led team, Radicubs relies on sponsorships and community support for competition fees, materials, tools, travel, outreach, and workspace.</p></AnimatedSection></div></section><section className="section"><div className="wrap"><div className="two-col"><AnimatedSection><a className="support-card dark" data-tilt-card style={{display:'block'}} href={site.donateUrl} target="_blank" rel="noreferrer"><div className="iconbox">♥</div><h2 style={{fontSize:28,marginTop:24}}>Donate</h2><p style={{color:'#bbb'}}>Make a tax-deductible donation directly to the team.</p><span className="text-link green">Donate ↗</span></a></AnimatedSection><AnimatedSection delay={.08}><a className="support-card" data-tilt-card style={{display:'block'}} href="/contact"><div className="iconbox">↔</div><h2 style={{fontSize:28,marginTop:24}}>Sponsor</h2><p className="muted">Partner with us as a corporate or individual sponsor.</p><span className="text-link green">Become a sponsor ↗</span></a></AnimatedSection></div><div style={{marginTop:80}}><p className="eyebrow">Partners</p><h2>Corporate Sponsors</h2></div><div className="corp-grid">{corporateSponsors.map((s,i)=><AnimatedSection key={s.name} delay={(i%6)*.05}><a className="sponsor-card" data-tilt-card href={s.href} target="_blank" rel="noreferrer"><div className="sponsor-logo"><img src={s.pageLogo ?? s.logo} alt={`${s.name} logo`} loading="lazy" decoding="async"/></div><div className="sponsor-card-copy"><div className="post-title">{s.name}</div>{s.note&&<div className="post-excerpt">{s.note}</div>}</div></a></AnimatedSection>)}</div><div style={{marginTop:80}}><p className="eyebrow">Community</p><h2>Individual Sponsors</h2></div><div className="individual-grid">{individualSponsors.map((n,i)=><AnimatedSection key={n} delay={(i%8)*.03}><div className="individual" data-tilt-card>{n}</div></AnimatedSection>)}</div></div></section><Footer/></main>}
