import type { Metadata } from "next";import { Navbar } from "@/components/sections/navbar";import { Footer } from "@/components/sections/footer";import { PostCard } from "@/components/blog/post-card";import { AnimatedSection } from "@/components/ui/animated-section";import { blogPosts } from "@/data/blog";
export const metadata:Metadata={title:"Blog | Radicubs"};
export default function BlogPage(){return <main><Navbar/><section className="page-hero"><div className="wrap"><AnimatedSection><p className="eyebrow">Radiblog</p><h1>Updates from Team 7503</h1><p className="section-copy">Build-season notes, robot progress, competition highlights, STEM stories, and outreach from the Radicubs archive.</p></AnimatedSection></div></section><section className="section"><div className="wrap"><div className="blog-grid">{blogPosts.map((p,i)=><AnimatedSection key={p.slug} delay={(i%9)*.025}><PostCard post={p}/></AnimatedSection>)}</div></div></section><Footer/></main>}
