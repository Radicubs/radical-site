# Fingerprints

Every site you build with **scroll-craft** gets one row here, appended after it
ships. The registry exists so your next build can prove it is a different page
rather than a re-skin of one you already made.

This file is **yours**. It starts empty on purpose: the gate is about not
repeating *yourself*, so it has nothing to say until you have built something.

The rules and the gate live in the skill's
`references/uniqueness.md`. Short version:

**A new build must differ from EVERY row below on at least 4 of the 6
dimensions.** Four against each row individually, not four on average across the
table. If a planned build fails, change the plan. Never edit a row to make room
for it.

The six dimensions are: **grammar**, **nav treatment**, **hero device**,
**act-sequence shape**, **close pattern**, **signature move**.

Dimension 6 is free, because a signature move is unique by definition. So the
gate really asks for three more out of the remaining five, and a build that
changes only grammar and world will fail it.

---

## The registry

| Build | Grammar | Nav treatment | Hero device | Act-sequence shape | Close pattern | Signature move | World | Port |
|---|---|---|---|---|---|---|---|---|

| Robot (photographic fallback, superseded) | Engineering inspection | Existing team nav + subsystem jump rail | Masked real photo with independent grid, floor, and type | One 7.6svh stage: overview, five focuses, resolve; notes in flow | Whole robot resolves, source ledger, team CTA | Source-registered inspection leaders around the actual competition robot | Real 2026 competition/workshop photography | 3100 |
| Robot v2 (machine teardown) | Machine teardown (authored) | Fixed separation ruler: a readout that reports how far apart the machine is and never navigates | Four-plane parallax hero, photographic cutout on a built room, claim above the machine in clear air | 9 acts, ~16.6svh, 8 device families, two scrubs at the two ends, peak 3.6 against a next-longest 2.4 | Reversed teardown, ruler collapses to a closed mark, page resolves onto a specification plate at its smallest type | The separation writes the labels: each supplied SVG title is drawn stroke by stroke at the rate its components leave the machine, and erased in reverse on reassembly | Real 2026 competition photography and a real exploded-view clip of the same machine | 3210 |

---

## What is taken

Add a bullet here whenever a build claims something a later build should avoid
reusing: a grammar, a nav treatment, a close pattern, a signature move, an
act-count-and-length band. The shared columns are what the next build inherits
as a constraint, so writing them down is the whole point.

- Machine teardown as a grammar, the separation ruler as a non-navigating readout, the specification-plate close, and the separation-writes-the-labels signature. Also the 9-act / ~16.6svh shape, which is deliberately outside the 8-to-14 reference: seven story acts were mandated by the brief and none is filler.
- Engineering inspection around an intact competition photograph with source-registered leader lines and a subsystem jump rail. First entry; no prior rows shared. Literal mechanical disassembly remains asset-dependent.

---

## Appending a row

After shipping, add one line to the table and one bullet to **What is taken** if
the build claimed something new. Fill every column. Say what the build shares
with existing rows.

Rows are append-only. A build that has been superseded stays in the table,
because the space it occupies is still occupied.

---

## Worked example

The skill's author kept a registry of twelve builds across eight page grammars.
If you want to see what a filled-in table looks like, and which shapes tend to
collide, read `EXAMPLES.md` in the scroll-craft repository. Treat it as
illustration only: those rows are somebody else's builds and they do **not**
constrain yours.
