import { AnimatedSection } from "@/components/ui/animated-section";
import { awards } from "@/data/awards";
export function HowItWorksSection(){return <section className="section"><div className="wrap"><AnimatedSection><p className="eyebrow">Awards</p><h2>Recognition earned across eight seasons.</h2><p className="section-copy">From rookie honors to creativity, sustainability, team spirit, and gracious professionalism.</p></AnimatedSection><div className="award-grid">{awards.map((a,i)=><AnimatedSection key={`${a.year}-${a.name}-${a.event}`} delay={(i%6)*.04}><a className="award" data-tilt-card href={a.href} target="_blank" rel="noreferrer"><div className="award-image"><img src={a.image} alt={`${a.name} artwork`} loading="lazy" decoding="async"/></div><div><div className="award-year">{a.year}</div><div className="award-name">{a.name}</div><div className="award-event">{a.event}</div></div></a></AnimatedSection>)}</div></div></section>}
