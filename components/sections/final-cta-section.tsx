import BorderGlow from "@/components/BorderGlow";
import { AnimatedSection } from "@/components/ui/animated-section";
import type { SiteSettings } from "@/lib/cms";
export function FinalCtaSection({settings}:{settings:SiteSettings}){return <section className="section"><div className="wrap"><AnimatedSection><BorderGlow borderRadius={44} backgroundColor="#0a0f0b" glowColor="118 100% 65%" colors={["#66ff55","#00c700","#0a2e12"]} glowIntensity={1.1} coneSpread={30} fillOpacity={0.35}><div className="cta-grid"><div><p className="eyebrow">Join or support Radicubs</p><p>Applications for the {settings.applicationSeason}–{settings.applicationSeason + 1} season are open. Donations to the team support a student-led 501(c)(3) nonprofit.</p></div><div className="actions"><a className="btn btn-green" href={settings.applyUrl} target="_blank" rel="noreferrer">Apply now →</a><a className="btn btn-light" href={settings.donateUrl} target="_blank" rel="noreferrer">Donate</a></div></div></BorderGlow></AnimatedSection></div></section>}
