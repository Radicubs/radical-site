import Link from "next/link";
import { BlogPost } from "@/data/blog";
export function PostCard({post}:{post:BlogPost}){return <Link className="post-card" data-tilt-card href={`/blog/${post.slug}`}><div className="post-img">{post.cover?<img src={post.cover} alt={post.title} loading="lazy" decoding="async"/>:<div style={{height:'100%',display:'grid',placeItems:'center',padding:30,textAlign:'center',fontWeight:700}}>{post.title}</div>}</div><div className="post-body"><div className="post-date">{post.date}</div><div className="post-title">{post.title}</div><div className="post-excerpt">{post.excerpt}</div><div className="text-link" style={{marginTop:18}}>Read post ↗</div></div></Link>}
